python-dotenv
hume[stream]
hume[microphone]
opencv-python
streamlit

# Voice Recognition
speechrecognition
pyaudio

#run this before below if error
#! pip install --upgrade setuptools
distutils

# For some reason if there is an ffmpeg error (windows), include:
# pip install ffmpeg
# refer to https://github.com/jiaaro/pydub/issues/348